"use client";

import { useEffect, useState } from "react";
import { Excalidraw } from "@excalidraw/excalidraw";
import "@excalidraw/excalidraw/index.css";
import { useSearchParams } from "next/navigation";

export default function Phase1Whiteboard() {
  const searchParams = useSearchParams();
  const [mode, setMode] = useState<string | null>(null);

  // Wait until searchParams are ready
  useEffect(() => {
    setMode(searchParams.get("mode"));
  }, [searchParams]);

  const isStudent = mode === "student";

  // Log to browser console
  console.log("Phase1Whiteboard mode:", mode, "isStudent:", isStudent);

  return (
    <div style={{ height: "100vh", position: "relative" }}>
      {/* Mode badge */}
      <div
        style={{
          position: "fixed",
          top: 12,
          right: 12,
          zIndex: 9999,
          background: isStudent ? "#333" : "#0b5",
          color: "#fff",
          padding: "6px 12px",
          borderRadius: 6,
          fontSize: 12,
          fontWeight: 600,
        }}
      >
        {isStudent ? "STUDENT VIEW (Read only)" : "TUTOR VIEW (Editable)"}
      </div>

      {/* Excalidraw: locked if student */}
      <Excalidraw viewModeEnabled={isStudent} />
    </div>
  );
}
